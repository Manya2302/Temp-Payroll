Create a complete full-stack MERN project named Loco (Payroll Management System inspired by Zoho Payroll) using only JavaScript (no TypeScript) with React for frontend, Node.js + Express for backend, MongoDB for database, and include authentication (login, registration, JWT sessions) for Admin and Employee roles. Implement Admin Panel with features: manage employees (add, update, delete, view), manage payrolls (add new payroll records, update, delete, list salaries), approve/reject employee leave requests, view employee statistics, and CRUD operations for payments (add, edit, delete, list). Implement Employee Dashboard with profile management (view/update profile, password change), leave management (apply for leave, view leave status pending/approved/rejected), payroll overview (view salary slips, pending payments, deductions and allowances breakdown), and simple statistics for their activity. Include CRUD pages for all modules (employee, payroll, payments, leave requests, reports) as separate React .js files like Login.js, Register.js, AdminDashboard.js, EmployeeDashboard.js, EmployeeList.js, AddEmployee.js, EditEmployee.js, PayrollList.js, AddPayroll.js, EditPayroll.js, PaymentList.js, AddPayment.js, EditPayment.js, LeaveRequests.js, ApplyLeave.js, Profile.js, Reports.js, and ensure clean navigation with React Router. Add secure backend with Express routes for auth, employees, payrolls, payments, and leaves; protect endpoints with JWT middleware; hash passwords with bcrypt; sanitize inputs to prevent XSS/SQL injection; and apply role-based access (admin vs employee). Design frontend with React + TailwindCSS for clean dashboards, employee cards, payroll tables, and leave request panels. Provide REST APIs in backend for all CRUD operations. Ensure email/password login and session-based protection. The system must be modular, with backend folders /models, /routes, /controllers, /middleware and frontend files in /src/pages with one .js file per page for simplicity. Add comments in each file so the structure is beginner-friendly, and make it fully working with MongoDB connection, secure auth, CRUD operations, leave approval flow, payment CRUD, and dashboards for both Admin and Employee.